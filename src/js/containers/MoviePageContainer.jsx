import React, { Component } from 'react';
import { connect } from 'react-redux';

class MoviesPageContainer extends Component {
    render() {
        <div>
            Movies page
        </div>
    }
};

const mapStoreToProps = store => {
    return {
        
    }
};

export default connect(mapStoreToProps)(MoviesPageContainer);